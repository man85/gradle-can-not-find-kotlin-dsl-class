Reproducer for:

```kotlin
Unresolved reference. None of the following candidates is applicable because of receiver type mismatch: public val PluginDependenciesSpec.base: PluginDependencySpec defined in org.gradle.kotlin.dsl
```

Run `build.sh` and wait for a few hours to reproduce.
